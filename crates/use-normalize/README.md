# use-normalize

Whitespace and simple text normalization helpers.

`use-normalize` provides explicit helpers for collapsing whitespace, trimming lines, removing empty lines, and ensuring exactly one trailing newline.
