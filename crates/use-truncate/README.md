# use-truncate

Character- and word-safe text truncation helpers.

`use-truncate` truncates by Rust `char` or by whitespace-delimited words. Ellipsis helpers use the single-character ellipsis `…` and never split UTF-8 bytes.
