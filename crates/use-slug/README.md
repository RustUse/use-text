# use-slug

Simple ASCII slug generation helpers.

`use-slug` lowercases ASCII letters, keeps ASCII alphanumeric characters, collapses separator runs, and trims leading or trailing separators.

## Unicode behavior

For v0.1, non-ASCII characters are dropped rather than transliterated.
