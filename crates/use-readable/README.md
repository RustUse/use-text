# use-readable

Basic readability and text metric helpers.

`use-readable` counts words, characters, bytes, and sentence-ending punctuation, then estimates reading time with a small deterministic heuristic.
